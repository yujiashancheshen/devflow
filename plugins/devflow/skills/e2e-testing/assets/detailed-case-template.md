# {{CASE集合名称}}

**来源**：{{PRD/概要设计/用户描述路径或摘要}}

**执行范围**：{{本次包含的 Case/功能范围}}

**不在范围**：{{明确不执行的内容；无则写无}}

## 运行上下文

- Base URL：`{{baseUrl}}`
- 业务作用域：`{{scopeName}}`
- 执行角色/权限：`{{role}}`
- 运行时参数：{{参数名与用途}}
- 运行时秘密：{{只写参数名，不写值}}
- 数据生成/复用规则：{{稳定规则}}

## Case 清单

### {{CASE-ID}} {{CASE 名称}}

**覆盖需求**：{{需求编号/验收条款}}

**前置条件**

- {{前置条件 1}}
- {{前置条件 2}}

**步骤**

1. {{用户可见操作 1}}
2. {{用户可见操作 2}}
3. {{记录需要传给后续步骤的 ID/状态}}

**预期结果**

- {{可见页面结果}}
- {{关键状态/数量/变化量}}
- {{最终断言}}

**副作用与恢复**

- 不可逆操作：{{无/操作说明}}
- Checkpoint：{{无/成功后记录的稳定 ID}}
- 重跑规则：{{可从头执行/仅可从 checkpoint 恢复}}

> 对每个 Case 重复上述结构，保留原始 Case ID。

## 步骤与 Case 映射

| 脚本步骤键 | 对应 Case | 说明 |
| --- | --- | --- |
| `{{step-key}}` | `{{CASE-ID}}` | {{该步骤证明的 Case 行为}} |

## 通过标准

- 每个选中 Case 都有明确结果：`通过/失败/阻塞/未执行`。
- Case 通过时，所有必需映射步骤和最终断言均成功。
- 失败 Case 记录失败步骤、预期、实际和证据。
- 环境/鉴权/输入导致未达 Case 断言时标记为阻塞，不标记为产品失败。
