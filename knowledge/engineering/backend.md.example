# 后端工程规范示例

> 适用于 Go/Gin 后端服务。按实际技术栈补充，以下为通用条目和参考结构。

## 技术栈选型

| 层级 | 推荐选型 | 说明 |
|---|---|---|
| Web 框架 | Gin | 内部封装版通过 replace 指令替换 |
| ORM | GORM + MySQL | 数据访问 |
| 消息队列 | RocketMQ / Kafka | 按团队实际选择 |
| 缓存 | Redis | 优先使用高级封装，避免裸指令 |
| CLI | Cobra | 多入口支持 |
| 配置 | YAML + 环境变量注入 | 统一配置目录 |

## 分层与目录约束

| 层 | 目录 | 职责 | 禁止 |
|---|---|---|---|
| 路由层 | `router/` | 路由注册、中间件挂载 | 业务逻辑 |
| 控制器层 | `controllers/` | 参数绑定、调用 service、渲染响应 | DB/Cache 操作 |
| 业务逻辑层 | `service/` | 核心业务编排、事务、幂等 | 直接操作 DB |
| 领域层 | `domain/` | 写操作收敛，含事务与补偿 | 直接操作 DB |
| 数据访问层 | `models/` | DB CRUD | 业务逻辑 |
| 公共能力层 | `data/` | MQ、缓存、横切能力 | 业务逻辑 |
| 下游客户端 | `api/` | 外部 RPC/HTTP 调用 | 业务逻辑 |

## Domain 层核心模式

- 一个 domain 目录对应一个业务域，按 `dm<domain>` 命名。
- Aggregate 只暴露 `XXXCmd()` 入口，一个 cmd 一个事务。
- 固定执行顺序：`checkInput → prepareData → beforeTxRun → db.Transaction → rollbackBeforeTxRun → afterTxRun`。
- 事务内禁止 RPC/MQ，事务外动作放 beforeTxRun 或 afterTxRun。
- cmd struct 字段避免指针类型，用 `map[ID]Model + TheXXID` 组合。

## 入口与接口

- 接口 path 格式：`/模块名/网关域/xx`，网关域分 mis（内网）、public（公网）、api（服务间调用）。
- 服务间调用统一走 api 域，不跨域调用。
- MQ 消费者配置 orderly + retry，配置文件在统一配置目录。

## 数据与事务

- 公共代码下沉到 data 层，按业务实体分目录。
- 事务不可嵌套，事务内禁止 RPC。
- 批量查询必须设 Limit 或分批游标，避免 OOM。
- 时间字段统一使用时间戳，避免 time.Time 受时区影响。

## 错误与日志

- 参数验证失败必须精确指出参数名和原因，禁止笼统报错。
- 资源操作异常必须记录日志，包含上下文（请求 ID、业务主键）。
- 默认使用 Warn 级别，Error 级别需备注原因。
- 敏感字段需注册脱敏 hook。

## 质量红线

- 模型字段严格遵从 DDL，禁止凭空推断。
- API 修改保持向后兼容，新增字段允许为空。
- Context 必须透传，禁止斩断链路。
- 所有入口操作必须有幂等保护。
- 废弃代码直接删除，不注释保留。
- 错误日志必须包含原始错误信息。

## 测试要求

- Domain cmd 文件与测试文件一一对应，至少覆盖 8 种场景（入参非法、前置失败、事务失败、补偿、幂等等）。
- Service 层单测极简：构建入参直接调用，不过度 Mock。
- Data 层和 API 层所有公共方法必须提供单元测试。
