# 前端工程规范示例

> 按实际技术栈补充，以下为通用条目和参考结构。

## 技术栈与文件类型

- 业务源码使用 TypeScript，不在源码目录生成 JS 文件。
- 框架 SFC 使用 `<script setup>` 语法。

## 代码风格

- 统一缩进（2 空格）、引号、分号规则，由格式化工具统一。
- 优先保持目标仓库现有格式。
- 不在代码审查中手动争论格式。

## 组件与公共能力

- 公共组件接口、命名、导出路径和参数要稳定，避免破坏式改动。
- 组件划分：单一职责，可复用但不过度抽象。
- 公共组件按功能或领域分目录。

## 状态管理与请求

- 先核对目标仓库的路由、组件、状态管理、请求封装和设计资源，再开始实现。
- 状态管理方案沿用项目现有方案，不引入新库。
- 接口调用统一走项目封装的请求层。

## 设计资源

- 实现前确认设计稿或设计系统。
- 图标优先使用项目已有图标库。
