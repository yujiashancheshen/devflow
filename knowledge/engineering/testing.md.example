# 测试策略

测试优先证明用户或调用方可观察的行为，不以函数覆盖率或测试数量作为目标。

## 后端

- 默认从 HTTP、RPC、MQ、定时任务或一次性任务等真实业务入口发起测试。
- 保留入口后的真实业务链路，只 Mock 数据库、Redis、外部 API、MQ、文件系统、时钟等外部 IO 或进程边界。
- 验证响应、错误码、持久化动作、消息发送、幂等结果和其他可观察副作用。
- 不通过 Mock controller 后的 service、domain 等内部实现制造接口测试通过，除非该调用在当前架构中本身就是明确的外部边界。
- DTO 转换、简单转发、getter、框架胶水和已被入口测试充分覆盖的内部函数，默认不新增单独测试。
- 复杂纯逻辑、算法、解析器、状态计算或需要精确复现的缺陷回归，可以补充函数级单元测试。

## 前端

- 默认使用类型化 fixture 或 API Mock，从真实页面入口验证页面行为。
- 通过用户操作验证加载、正常、空数据、失败、权限、禁用、表单提交、弹窗、跳转和刷新等本次需求涉及的状态。
- 断言用户可观察的页面结果、请求参数和关键副作用，不绑定组件内部实现细节。
- 普通组件、hooks、computed 和工具函数已被页面测试充分覆盖时，默认不重复新增小粒度测试。
- 复杂纯逻辑、公共状态计算、关键公共组件或明确缺陷回归，可以补充更小粒度测试。
- 项目没有适用自动化框架时，不为单个页面强行引入大型测试基础设施；使用现有页面测试、E2E、可重复人工验证或构建检查，并如实记录限制。

## 通用约束

- Mock 和 fixture 只能存在于开发或测试上下文，不得进入生产路径。
- 测试必须覆盖详细设计中的验收场景；缺少测试只有在会掩盖当前改动的具体行为风险时才构成 Review Finding。
- 未实际执行的命令不能写成通过，修复代码后必须重新执行受影响验证。
- 跨仓需求在测试环境执行连续业务场景，测试数据沿场景传递；该验证属于 E2E 阶段，不用单仓函数测试替代。
