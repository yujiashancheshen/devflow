# 场景导航示例

记录一个业务场景落在哪些模块、页面和代码入口，只做定位，不写具体实现。

## 参考结构

- **场景清单**：表格列出场景名称、涉及模块（仓库名）、关键页面路径、后端入口。
- **跨系统链路**：对需要多个系统配合的场景，按调用顺序列出经过的模块和衔接方式。

| 场景 | 涉及模块 | 关键页面 | 后端入口 |
|---|---|---|---|
| 示例：新建某业务单据 | repo-a、repo-b | /example/create | POST /api/example/create |

## 编写要点

- 只回答"在哪里"，不回答"怎么实现"。字段、参数和具体逻辑一律以代码为准。
- 模块名用真实仓库名，页面用真实路由，便于 AI 直接定位。
- 跨系统场景要标清顺序和衔接点，E2E 阶段据此生成跨系统的 case 流。
- 场景粒度对齐产品功能，不要按接口拆得过细。
- 场景增减频繁的领域不必全量登记，只保留会被反复复用的主链路。
