# 任务中心接入示例

> 示例：异步任务平台的接入约定。适用于需要统一管理导入/导出任务的场景。

## 参考结构

- **何时使用**：明确该平台的适用场景和边界。
- **接入流程**：按步骤列出申请和配置顺序，不需要开发平台仓库代码。
- **必要规则**：列出不可违反的硬性约束。
- **配置入口**：提供后台管理地址和仓库引用。

## 编写要点

- 区分任务类型（导入/导出），不同类型使用不同配置。
- 列出申请流程：任务类型 ID → 模板配置 → 页面配置 → 灰度规则 → 业务实现 → 联调。
- 关键 ID 在环境间通用，不要重复申请。
- 业务逻辑由业务服务负责，平台只负责通用管理。
