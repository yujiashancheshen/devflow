# 模块边界示例

一个 Git 仓库就是一个模块。需求分析和技术方案阶段，先用本文件定位仓库，再进入仓库确认真实代码、接口和依赖。

## 参考结构

- **仓库职责地图**：表格列出每个仓库名称和主要职责，用于路由和任务拆分。
- **专题架构知识**：对跨仓库的专题（如任务中心、消息网关）链接到独立文档。
- **判断规则**：明确知识库的定位——提供导航和约束，不替代代码验证。

## 编写要点

- 职责描述要粗粒度，一个仓库一句话，不展开到具体接口。
- 未确认的职责不要从名称推断，标注待确认。
- 跨仓库需求在方案中明确每个仓库的改动、调用关系和交付顺序。
- 知识库与代码冲突时，记录冲突并请求确认。
